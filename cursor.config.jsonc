{
    // 1. Tell the editor where your entry point is
    "projectRoot": ".",
    
    // 2. AI settings: preload ensures these files are read into every prompt
    "ai": {
    "preload": [
      { "path": "./cursor.local.jsonc",       "optional": true           },  // Optional local Cursor config
      { "path": "./rules/column-context.md",  "alias": "column-context"  }
      ]
    }
  }
  